import React from 'react';
import SearchForm from './SearchForm';

function App() {
  const handleFormSubmit = (formData)=> {
  console.log('FormData', formData);
  };
  return (
    <div className="App">
      <h1>Search Migrator</h1>
      <SearchForm onSubmit={handleFormSubmit} />
    </div>
  );
}

export default App;

